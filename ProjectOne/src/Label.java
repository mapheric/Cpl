
public class Label {

	public static String name(String s) {
		//cases: Relational Operator, identifier, num, keyword, error
		//remove whitespaces
		//if isNumber is true...
		//if isLetter is true.. check if keyoword; else is identifier
		//if relational operator
		//else is error
		
		return "no";
	}
}
